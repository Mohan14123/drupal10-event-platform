# msmtp configuration for development
# Logs all emails to /tmp/mail.log instead of sending

defaults
auth           off
tls            off
logfile        /tmp/mail.log

# Default account (logs to file)
account        default
host           localhost
port           25
from           noreply@localhost

# For production, configure with real SMTP:
# account        production
# host           smtp.example.com
# port           587
# auth           on
# user           your-smtp-user
# password       your-smtp-password
# tls            on
# tls_starttls   on
# from           noreply@yourdomain.com
